import type { ReactElement } from "react";

export type ProjectsActionsProps = {
	children: (projectId: string) => ReactElement;
};

export const ProjectsActions = (_props: ProjectsActionsProps) => {
	return null;
};
