import type { ReactElement } from "react";

export type ProjectsListActionsProps = {
	children: (projectId: string) => ReactElement;
};

export const ProjectsListActions = (_props: ProjectsListActionsProps) => {
	return null;
};
